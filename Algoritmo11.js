/**
 11) Algoritmo que nos diga si una persona puede acceder a cursar un ciclo formativo 
 de grado superior o no. Para acceder a un grado superior, si se tiene un titulo de bachiller, 
 en caso de no tenerlo, se puede acceder si hemos superado una prueba de acceso. 
 
 */

let TB=0;
let x=0;
alert("Algoritmo 11--La Persona puede acceder a cursar un ciclo formativo de educacion superior");

 TB=parseInt(prompt("Si Tiene Titulo Bachiller dijite=1 , sino digite=2")); 
 
    if (TB==1) {
        alert( "Puede Acceder al Grado Superior");
              } 
    else {
    
   X= parseInt(prompt("seleccione la respuesta correcta Cual es la  respuesta correcta ,cual es el valor de x si a=4 de la siguinte expresion a*a+2=x  "));
  if (x==18) {
    alert( "Puede Acceder al Grado Superior");
         } 
  else 
        {
    alert( "No Es Apto para el Grado Superior");
        }

    }
